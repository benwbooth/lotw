[package]
name = "lotw-runtime"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true

[features]
sdl = []

[dependencies]
lotw-port = { path = "../lotw-port" }
